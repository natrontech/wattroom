[Unit]
Description=Check the WattRoom release pin every five minutes

[Timer]
OnBootSec=2min
OnUnitActiveSec=5min
# The pin does not move on a schedule, so a deploy landing up to half a minute
# late costs nothing and lets systemd batch the wakeup.
AccuracySec=30s

[Install]
WantedBy=timers.target
